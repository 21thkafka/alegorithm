package algorithm;

public class basicSort {
	//정렬
	/*
	 * Selection Sort:가장 큰 값을 찾아서 맨 끝으로 보내고 그 다음 큰 값을 찾아 끝에서 두번째 자리를 보내는 작업 반복
	 * selectionSort(A[], n)
	 * {
	 * 		for last <- n downto 2 {
	 * 			A[1...last] 증 가장 큰 수 A[k]를 찾는다;
	 * 			A[k] <-> A[last]; A[k]와 A[last]의 값을 교환
	 * 		}
	 * }
	 */
	/*Bubble Sort
	 * bubbleSort(A[], n)
	 * {
	 * 		for last <- n downto 2 {
 				for i < 1 to last-1
 					if (A[i]>A[i+1]) then A[i] <-> A[i+1];
 	 * }
	 * 
	 */
	/*
	 * Insertion Sort
	 * insertionSort(A[], n)
	 * {
	 * 		for i <- 2 to n {
	 * 			A[1...i]의 적당한 자리에 A[i]를 삽입한다.
	 * }
	 */
	

}
